services:
  convert:
    build:
      context: ..
      args:
        VITE_COMMIT_SHA: docker
      dockerfile: docker/Dockerfile
    image: bromn-convert:latest
    ports:
      - "8080:80"
    restart: unless-stopped
